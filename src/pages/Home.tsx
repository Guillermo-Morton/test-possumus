import React from 'react';

const Home = ({className}: {className: string}) => {
    return (
        <div className={`${className}`}>
            <h1>Hola</h1>
        </div>
    );
};

export default Home;